# --- core ---
torch>=2.4.0
torchvision
torchaudio
numpy<2          # numpy 2.x breaks torch 2.4 ABI on RunPod
pyyaml
python-dotenv
tqdm

# --- script (LLM) ---
anthropic>=0.40.0
openai>=1.40.0

# --- GUI (web interface) ---
gradio>=4.44.0

# --- voice (TTS) ---
# Kokoro: light, great American voices, runs on CPU or GPU
kokoro>=0.9.2
misaki>=0.9.2          # G2P for Kokoro (English)
soundfile
sentencepiece          # required by the Albert text encoder Kokoro uses
# NOTE: also needs the OS package `espeak-ng` (installed by setup.sh)
# F5-TTS alternative (voice cloning) — uncomment to use:
# f5-tts

# --- captions (word-level timing) ---
whisperx>=3.1.1

# --- AI video ---
diffusers>=0.32.0
# transformers 4.50+ needs torch 2.7+ (uses torch.float8_e8m0fnu).
# RunPod ships torch 2.4.1, so keep transformers in a compatible range:
transformers>=4.47.0,<4.50
accelerate
imageio
imageio-ffmpeg

# --- assembly + thumbnail ---
ffmpeg-python
pillow

# Note: ffmpeg binary must also be installed at the OS level and on PATH.
